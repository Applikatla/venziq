import { Section, Eyebrow } from '../components/Section'

export function CorePattern() {
  return (
    <Section id="use-cases" panel>
      <Eyebrow>The core pattern</Eyebrow>
      <h2 className="max-w-3xl text-4xl font-semibold md:text-5xl">
        AI action + ZK verification + blockchain audit = trusted autonomy
      </h2>
      <p className="mt-6 max-w-2xl text-muted">Placeholder — equation and use cases.</p>
    </Section>
  )
}
